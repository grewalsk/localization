"""KV-compression flip-test (the payoff).

Eviction sweep via an external library + reproduce-paper gate (§9.1),
pre-registered correctness metric (§9.2), and the leakage-safe flip-prediction
model (§9.3). ``D(x)`` is computed strictly from the full-cache run.
"""

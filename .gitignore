# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/

# uv lockfile — regenerated locally on `uv run`/`uv sync`. Committed only once
# the full GPU env (incl. CUDA torch sources) is first known-good so the parity
# gate 11.0 is reproducible (addendum §1.2). Remove this ignore at that point.
uv.lock

# Models / checkpoints / caches (re-downloadable; never commit)
*.safetensors
*.bin
*.pt
checkpoints/
.cache/
hf_cache/

# Results / scratch
outputs/
results/
figures/*.png
*.log

# OS / editor
.DS_Store
.ipynb_checkpoints/
.idea/
.vscode/
